version: 2

sources:
  - name: public
    tables:
      - name: payment
        config:
          tags: [ 'PII' ]
        tests:
          - elementary.volume_anomalies
        columns:
          - name: payment_id
            tests:
              - accepted_values:
                  values: [ 1 ]
